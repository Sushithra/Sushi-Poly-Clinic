// Shared dashboard scaffold placeholder.
// Future responsibility: provide responsive widget groupings, stacked mobile cards,
// and tablet or desktop-ready analytics layouts.

export default function DashboardScaffold({ children }) {
  return <section className="dashboard-scaffold">{children}</section>;
}
