// Shared responsive content container placeholder.
// Future responsibility: standardize max widths, safe spacing, and adaptive inner padding.

export default function ContentContainer({ children }) {
  return <div className="content-container">{children}</div>;
}
